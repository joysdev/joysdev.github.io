Experimenting with ChatGPT in an attempt to move through my current block on coding an interval timer. Copy-pasted the results from asking "How to code an interval timer in JavaScript":

```<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interval Timer</title>
    <style>
        body { font-family: Arial, sans-serif; }
        #timer { font-size: 2em; }
    </style>
</head>
<body>

<div id="timer">0</div>
<button id="startButton">Start</button>
<button id="stopButton">Stop</button>

<script>
    let timerInterval;
    let elapsedTime = 0;

    const timerDisplay = document.getElementById('timer');

    function startTimer() {
        // Prevent multiple intervals from being created
        if (!timerInterval) {
            timerInterval = setInterval(() => {
                elapsedTime++;
                timerDisplay.textContent = elapsedTime;
            }, 1000); // Update every second
        }
    }

    function stopTimer() {
        clearInterval(timerInterval);
        timerInterval = null; // Reset the interval
    }

    document.getElementById('startButton').addEventListener('click', startTimer);
    document.getElementById('stopButton').addEventListener('click', stopTimer);
</script>

</body>
</html>
```
which did not result in what I wanted. It only has one input and counts up, that is, from 0. So I would call that just a timer, and if that was what I found in my phone's timer app, I would be disappointed and rarely use it. 

I can see how asking AI these kinds of questions could help me find built-in JavaScript functions-if that's even the correct term. It is not. Apparently there are only a handful of those; I'm looking for a different term. Like libraries-maybe?


It just occurred to me how helpful it would be to have a Javascript anatomy map. Then I thought what I really need is a recipe-like start with these ingredients, put this in, then add that. And fazaa, I found a title available at my public library: "JavaScript Cookbook". Hopefully it is what I imagine... Also I recognize I may be falling into a trap of putting books on hold instead of actually coding. 


Back to troubleshooting on my interval timer. I can't seem to get back to where my poseTime would add block style. I have been reviewing my earlier commits, and I don't know what I'm missing. Apparently I needed to make sure all files were reverted. I still don't know why my better-written code doesn't work as well as my older mess, but I'm leaving tonight on this high-ish note.
