Working through the JavaScript kids book. Stuck on this:

allowedIn = age >=13 || accompanied = true;
var age = 12;
var accompanied = true;
allowedIn
VM799:1 Uncaught SyntaxError: Invalid left-hand side in assignmentUnderstand this error

Why doesn't this return allowedIn true or false?

added this:

if(age >= 13 || accompanied = true) {
    console.log(admit)
};

and got this: 


VM1060:1 Uncaught SyntaxError: Invalid left-hand side in assignment

Idek what to ask... like "How to get JavaScript to answer a Boolean?"

Tried and got this:

let allowedIn = age >=13 || accompanied = true;
var age = 12;
var accompanied = true;
console.log(allowedIn);
VM1231:1 Uncaught SyntaxError: Invalid left-hand side in assignment

And changed my let to const and tried var, too, and still no dice. Tried return instead of console.log(). I am completely lost. I can't afford to cry right now...

I suspect the answer is something that I don't try because I can see the age is fine-I don't understand how JavaScript makes anything easier, yet. Like I can look at the age of 12 and see accompanied is true and calculate it myself immediately. Why do we use JavaScript in cases like this where it takes longer and is harder to understand??

According to this answer to "How to get JavaScript to answer a Boolean with two variables"

let isSunny = true;
let isWarm = false;

let shouldGoToBeach = isSunny && isWarm; // false

My 

var age = 12;
var accompanied = true;
var allowedIn = age >= 13 || accompanied = true;

should work! But I get:

VM1598:3 Uncaught SyntaxError: Invalid left-hand side in assignment

And I don't get why VM codes are not researchable... Beginning to believe there is a flaw in my about:blank page...

This finally worked, and I think I can see why, but I still don't understand why anyone would choose such a finicky system when I could determine the correct answer myself in an instant. I also question how this is an appropriate question for a kids' exercise. 

var age = 12;
var accompanied = true;    

var allowedIn = (age < 13 && accompanied) || age >= 13;

allowedIn
true

